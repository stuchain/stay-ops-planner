generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Placeholder until core domain tables land in Phase 2.
model SchemaBootstrap {
  id String @id @default(cuid())
}
