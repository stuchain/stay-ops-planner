{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable pnpm && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && corepack enable pnpm && pnpm --filter @stay-ops/db exec prisma migrate deploy && pnpm --filter @stay-ops/db exec prisma migrate status && pnpm build:web",
  "build": {
    "env": {
      "SKIP_STAYOPS_ENV_VALIDATE": "1"
    }
  },
  "crons": [{ "path": "/api/cron/sync-hosthub", "schedule": "0 * * * *" }]
}
